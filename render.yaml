services:
  - type: web
    name: eazyride-admin
    runtime: static
    buildCommand: npm install --include=dev && npx vite build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
